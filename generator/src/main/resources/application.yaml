spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///db_jxwxkj_02?useSSL=false&serverTimezone=UTC
    username: root
    password: root

logging:
  level:
    root: warn
    com.example: trace
  pattern:
    console: '%p%m%n'